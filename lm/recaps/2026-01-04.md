# 2026-01-04

- Opened the day and logged the need to investigate missed updates on the boxes.
- Updated the host_vars example to list all required role variables and match per-box filenames.
- Forced `pi-base` to refresh the apt cache every run to avoid stale upgrade checks.
