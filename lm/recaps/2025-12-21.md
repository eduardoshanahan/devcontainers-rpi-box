# Recap for 2025-12-21.md

- Included `community.docker` in `requirements.yml`.
- Created `docker_smoke_test` role to deploy a transient `whoami` container for verification.
- Implemented `backup_restic` role for automated daily backups of `/srv/docker`.
- Updated `pi-base` playbook to include both smoke testing and backup services.
- Established a pattern for validating Docker networking and cleanup automatically.
- Verified the entire stack (idempotence + smoke checks + backup installation) on `rpi_box_01`.
