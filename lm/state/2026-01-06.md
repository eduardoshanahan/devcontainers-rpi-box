## State (2026-01-06)

- Updated docs to reference the correct inventory group (`raspberry_pi_boxes`).
- Documented `antigravity` as a valid `EDITOR_CHOICE`.
- Removed devcontainer fallback env defaults to keep `.env` as the only source of truth.
- Synced `.env.example` with current env structure, including backup schedule and OpenAI key placeholders.
- Updated environment variable docs to cover backup timer and OpenAI key usage.
- Added devcontainer image cleanup script and documented retention config in `.env.example`.
- Updated host vars example to read most values from `.env` lookups.
- Added PROJECT_NAME/tooling version validation and aligned PROJECT_NAME format docs.
- Added Docker availability checks to CLI launchers.
- Removed unused `.devcontainer/scripts/load-env.sh`.
- Made `GIT_REMOTE_URL` optional in env validation to match the reference.
- Aligned devcontainer prompt setup with the reference (starship config + login shell sourcing).
- Updated starship symbols to match the reference prompt.
- Synced starship configuration with the reference prompt layout.
- Added git-filter-repo to the devcontainer image.
- Replaced devcontainer helper scripts with reference versions for parity.
- Aligned devcontainer.json with the reference (image, runEnvFile, extensions, container env).
- Switched back to runArgs env-file after runEnvFile validation failed.
- Removed devcontainer.json image property to satisfy schema validation.
