# Recap for 2026-01-06

- Fixed inventory group name in docs to match `raspberry_pi_boxes`.
- Updated environment variable docs to include `antigravity` as an editor option.
- Dropped devcontainer fallback env defaults so `.env` stays authoritative.
- Aligned `.env.example` and env docs with current backup schedule and OpenAI key fields.
- Added devcontainer image cleanup helper and documented the retention setting.
- Converted host vars example to use env lookups for shared settings.
- Aligned env validation to require PROJECT_NAME and tooling versions; clarified PROJECT_NAME format.
- Added Docker running checks to the devcontainer and Claude launchers.
- Removed unused `.devcontainer/scripts/load-env.sh`.
- Made `GIT_REMOTE_URL` optional in env validation.
- Matched devcontainer prompt behavior with the reference (starship config + login shell sourcing).
- Updated starship prompt symbols to the reference glyphs.
- Synced starship config to match the reference layout.
- Added git-filter-repo to the devcontainer image.
- Aligned devcontainer helper scripts with the reference versions.
- Matched devcontainer.json settings with the reference layout.
- Reverted to runArgs env-file because runEnvFile is not supported.
- Dropped devcontainer.json image property after validation errors.
