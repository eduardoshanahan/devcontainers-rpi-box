# Recap for 2026-01-09

- Aligned devcontainer workspace mount target with `${localEnv:WORKSPACE_FOLDER}` to match `workspaceFolder`.
- Switched SSH agent forwarding to the reference pattern (bind-mount host socket to `/ssh-agent` and set `SSH_AUTH_SOCK=/ssh-agent`).
- Moved VS Code terminal profile settings into `customizations.vscode.settings` so VS Code applies them.
- Added `"shutdownAction": "stopContainer"` so the container stops when the VS Code window closes.
- Updated `.devcontainer/scripts/verify-git-ssh.sh` to require an explicit git host (arg or `GIT_SSH_HOST`) instead of defaulting.
- Restored `SSH_AUTH_SOCK` validation in `.devcontainer/scripts/validate-env.sh` to catch missing agent forwarding before container start.
- Updated `.devcontainer/scripts/post-create.sh` to run `.devcontainer/scripts/validate-env.sh`, add a VS Code shell integration guard in `~/.bashrc`, and align Claude install behavior with the reference.
- Updated `.devcontainer/scripts/bash-prompt.sh` to `set +u` for interactive shells to avoid VS Code shell integration nounset failures.
- Updated `.devcontainer/Dockerfile` to require pinned `ansible-core`, `ansible-lint`, and `yamllint` versions (no conditional fallbacks).
- Updated `.devcontainer/devcontainer.json` to pass `LOCALE` via `containerEnv`, and to stop using `--env-file` and `initializeCommand`.
- Updated `.devcontainer/devcontainer.json` to add the VS Code `anthropic.claude-code` extension without removing existing extensions.
- Updated `.devcontainer/scripts/validate-env.sh` to validate `CLAUDE_INSTALL_SHA256` when provided.
- Updated `.devcontainer/scripts/post-create.sh` to be fail-fast (`set -eu`) and to error if `validate-env.sh` is missing.
- Updated `.devcontainer/scripts/env-loader.sh` to require `.env`, preserve a valid `SSH_AUTH_SOCK`, and make debug output safer by default.
- Updated `.devcontainer/config/starship.toml` to show `CONTAINER_HOSTNAME` in the prompt when provided.
- Added ADR-0010 for the Claude Code devcontainer workflow and corrected the Traefik ADR numbering (now ADR-0009).
- Moved the daily journal from `lm/` to `documentation/diary/` and updated documentation references.
