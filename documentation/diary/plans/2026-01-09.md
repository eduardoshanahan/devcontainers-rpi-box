# Plans for 2026-01-09

- Rebuild/reopen the devcontainer to confirm workspace path, terminal profile, and SSH agent forwarding work end-to-end.
- Run `.devcontainer/scripts/verify-git-ssh.sh git@github.com` (or your actual host) to validate SSH auth in-container.
- Run `./scripts/ansible-smoke.sh` and confirm `yamllint` passes on `src/`.
