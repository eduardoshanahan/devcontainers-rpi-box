# State for 2026-01-08

- Working: systemd unit templates now flush handlers before enabling timers (daily report, docker prune, restic backup).
- Working: devcontainer CLI launcher now uses id-labels, builds images explicitly, skips Claude install, and has safer cleanup.
- Working: Claude launcher now uses id-labels, builds images explicitly, and has safer cleanup.
- Working: devcontainer Dockerfile now supports LOCALE build arg and uses a post-stop entrypoint.
