# State for 2026-01-07

- Added Docker service checks for docker-dependent roles.
- Added Docker Compose v2 availability checks for the smoke test role.
- Documented smoke test prerequisites in the testing guide.
- Replaced non-POSIX temp helpers and timestamps in shell scripts; removed timeout usage in post-create.
- Enabled Ansible host key checking by default and documented the override.
- Added a clear assert when the admin SSH public key file is missing in pi_base.
- Switched Starship init to sh and removed forced SHELL override in bash-prompt.
- Standardized diary date file headings to match directory instructions.
- Added a deployment verification role/playbook and documented how to run it.
- Renamed `scripts/sync_git.sh` to `scripts/sync-git.sh` and updated references.
- Updated `starship.toml` to match the reference configuration comments and structure.
- Switched devcontainer Ansible path settings to read from `.env` and validated them.
- Moved devcontainer workspace folder path to `.env` and added validation.
- Aligned `devcontainer-launch.sh` and `claude-launch.sh` with reference scripts.
- Switched Starship init to `bash` and exported `SHELL` in bash prompt setup.
- Renamed `launch.sh` to `editor-launch.sh` and updated documentation references.
